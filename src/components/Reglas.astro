---
const principles = [
  {
    title: "Tú eres el dueño",
    description:
      "El código, las bases de datos, los diseños y la documentación te pertenecen desde el primer día. Si quieres modificarlo, distribuirlo o venderlo, esa decisión es completamente tuya.",
  },
  {
    title: "Si quieres irte, te ayudamos.",
    description:
      "Si decides continuar con otro equipo, te entregamos todo: código limpio, documentación completa y asistencia para que la transición sea sin fricciones. Tu negocio no puede depender de que nosotros sigamos ahí.",
  },
  {
    title: "Tu idea es un secreto",
    description:
      "Tu problema, tu proceso y la solución que construimos juntos son confidenciales. Firmamos un acuerdo de confidencialidad antes de cualquier conversación que lo requiera. Lo que nos cuentas, se queda entre nosotros.",
  },
];
---

<style>
  .regla-item {
    opacity: 0;
  }
</style>

<section
  id="reglas"
  class="bg-[#191919] w-full"
  style="background-image: linear-gradient(rgba(255,255,255,0.05) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.05) 1px, transparent 1px); background-size: 48px 48px;"
>
  <div class="max-w-[1019px] mx-auto border border-white/30 border-solid">
    <!-- Header -->
    <div class="px-6 md:px-[51px] pt-[72px] pb-10 border-b border-white/20 flex flex-col gap-3">
      <h2 class="font-sans font-normal text-[30px] leading-[1.15] tracking-[-0.07em] text-white">
        Tres garantías que ponemos por escrito.
      </h2>
      <p class="text-base leading-relaxed tracking-[-0.07em] text-white/70 max-w-[511px]">
        Tres compromisos que nunca negociamos, sin importar el proyecto.
      </p>
    </div>

    <!-- Grid 2 cols -->
    <div class="grid grid-cols-1 md:grid-cols-2">
      {principles.map((p) => (
        <div class="regla-item px-6 md:px-[51px] py-10 flex flex-col gap-3">
          <h3 class="font-sans font-normal text-[30px] leading-[1.15] tracking-[-0.07em] text-white">
            {p.title}
          </h3>
          <p class="text-base leading-relaxed tracking-[-0.07em] text-white/70">
            {p.description}
          </p>
        </div>
      ))}
      {principles.length % 2 !== 0 && <div></div>}
    </div>
  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const items = document.querySelectorAll<HTMLElement>('.regla-item');

  items.forEach((item, i) => {
    gsap.fromTo(
      item,
      { opacity: 0, y: 20 },
      {
        opacity: 1,
        y: 0,
        duration: 0.6,
        ease: 'power2.out',
        delay: (i % 2) * 0.12,
        scrollTrigger: {
          trigger: item,
          start: 'top 85%',
          toggleActions: 'play none none reverse',
        },
      }
    );
  });
</script>
